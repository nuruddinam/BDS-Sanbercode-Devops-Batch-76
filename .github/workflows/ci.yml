name: CI Pipeline SanberCode

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Show Secret (Masked)
        run: |
          echo "Testing Secret Key..."
          echo "Secret is ${{ secrets.MY_SECRET_KEY }}"